# 约束 (Constraints)

> ⚠️ **必读文档**：任何任务都必须阅读本文档。约束不可被绕过。

---

## 设计原则

优先级裁决（冲突时按此顺序）：

```text
业务优先 > 职责优先 > 变更成本 > 简单优先 > 扩展优先
```

核心哲学：

```text
业务优先于技术
职责优先于分层
变更成本优先于开发速度
简单优先于复杂
扩展优先于修改
```

任何设计都应首先解决真实业务问题，而非追求某种架构风格、框架特性或技术概念。

> 上方优先级链为本哲学各项的简称，冲突时按此顺序裁决。

---

### 优先关注能力，而非数据

错误：

```text
用户管理
设备管理
订单管理
```

正确：

```text
设备发现
设备控制
设备同步
订单结算
订单履约
```

业务能力描述系统创造的价值，而非数据库中的实体。

---

### 职责清晰

每个模块必须能够回答：

```text
我负责什么？
我不负责什么？
```

如果一个模块存在多个变化原因，则说明职责划分存在问题。

---

### 避免过度设计

```text
Simple First, Evolve Later
```

- 不要为猜测中的需求设计
- 不要为了架构而架构
- 抽象必须来自真实变化，而非未来可能

三次法则：

```text
第一次：直接实现
第二次：允许重复
第三次：评估抽象
```

抽象升级条件：

- 出现真实重复
- 变化方向一致
- 抽象后降低复杂度

如果抽象后文件更多、调用链更长、理解成本更高，则禁止抽象。

---

### 为已知变化设计

如果变化已经明确存在（已进入需求规划、已确定会发生、已有多个实现方向），则应提前设计合理边界。

禁止为未知变化设计：

```text
以后可能... → 禁止
未来也许... → 禁止
万一... → 禁止
```

---

## 架构约束

### 三层隔离

逻辑分层，业务内聚、变更隔离、依赖单向。

```text
策略层（易变）
    ↓
核心层（稳定）
    ↑
技术层（可替换）
```

> **为什么知识库不需要详细业务文档？**
> 遵循三层隔离后，核心业务规则内聚在核心层，策略决策内聚在策略层，技术实现内聚在技术层。
> AI 可以通过代码结构精准定位业务逻辑，无需额外文档描述"哪个模块负责什么"。
> 知识库只需记录代码无法表达的内容：不变量、禁止事项、边界条件、术语定义。
> 如果核心层不够内聚（业务规则散落在技术层或策略层），则说明三层隔离未正确实施，应优先修复代码结构。

#### 架构状态

标注当前项目的三层隔离实施状态，AI 据此自动调整文档策略。

- 当前状态：<!-- 已实施 / 部分实施 / 未实施 -->
- 未隔离的模块：<!-- 列出业务逻辑与技术实现混合的模块 -->

| 架构状态 | AI 代码定位能力 | 文档策略 | 说明 |
|----------|----------------|----------|------|
| 已实施 | 精准：核心层内聚，AI 可直接定位 | 只记录负空间 | 代码结构足够清晰，无需文档补充定位 |
| 部分实施 | 部分：已隔离模块精准，未隔离模块模糊 | 未隔离模块补充定位信息 | 在文档中标注业务规则所在位置 |
| 未实施 | 模糊：业务逻辑散落各处 | 补充业务规则定位信息 | 文档需记录"哪个模块负责什么""业务规则在哪里" |

架构状态不是固定的。随着代码重构，状态从"未实施"→"部分实施"→"已实施"，文档应逐步精简，而非一直保持完整。

#### 模块定位（架构状态为"未实施"或"部分实施"时填写）

<!-- 以下内容仅在架构状态不是"已实施"时填写 -->
<!-- 已实施时此节可删除，AI 可从代码结构直接定位 -->

| 模块 | 负责什么 | 业务规则位置 | 不负责什么 |
|------|----------|-------------|-----------|
| <!-- 模块名 --> | <!-- 业务职责 --> | <!-- 哪个文件/类/函数 --> | <!-- 不做什么 --> |

| 层 | 职责 | 允许依赖 | 禁止出现 |
|----|------|----------|----------|
| 策略层 | 路由、调度、重试、选择逻辑 | 核心层、技术层 | 业务规则实现 |
| 核心层 | 能力契约、领域模型、业务规则、不变量 | 无外部依赖 | SQL、Redis、MQ、HTTP、框架代码 |
| 技术层 | 用具体技术实现核心层定义的端口 | 核心层 | 业务逻辑 |

#### 策略层

回答：**选择怎么做？**

负责：路由策略、调度策略、重试策略、采样策略、选择逻辑。

#### 核心层

回答：**能做什么？必须遵守什么？**

负责：能力契约、领域模型、业务规则、不变量。

不变量优先：流程可以变化，不变量不能被绕过。

#### 技术层

回答：**具体如何完成？**

负责：存储实现、通信实现、第三方系统接入。

技术层可替换（换数据库、换消息队列，核心层不受影响）。

---

### 依赖方向

| 源 | 目标 | 允许 | 说明 |
|----|------|------|------|
| 策略层 | 核心层、技术层 | 是 | 策略层调用核心层接口，组装技术层实现并注入核心层 |
| 核心层 | 技术层 | 否 | 核心层只定义端口，不依赖具体实现 |
| 技术层 | 核心层 | 是 | 技术层实现核心层定义的端口 |
| 核心层内 | 互相 | 否 | 核心层内禁止互相依赖 |

---

### 模块边界

- 每个模块对应一个明确的业务能力
- 模块间通过接口通信，禁止直接访问其他模块内部实现
- 模块内部可以自由组织，但对外暴露的接口必须稳定
- 跨模块数据访问必须通过接口，禁止直接查询其他模块的数据表

---

### 错误处理

- 核心层定义业务异常类型，携带业务语义
- 技术层将技术异常转换为业务异常
- 策略层决定错误恢复策略（重试、降级、熔断）
- 禁止吞掉异常（空 catch）
- 禁止在核心层使用技术异常类型（如 SQLException）

---

## 业务不变量

| 编号 | 不变量描述 | 检查位置 |
|------|-----------|----------|
| INV-001 | <!-- 描述 --> | <!-- 检查位置 --> |

---

## 禁止事项

### 架构禁止

- 核心层禁止出现 SQL、Redis、MQ、HTTP、框架基础设施代码
- 核心层内禁止互相依赖
- 禁止循环依赖
- 禁止隐式依赖
- 禁止全局状态共享
- 禁止任意跨模块访问

### 设计禁止

- 禁止为未知变化提前抽象（YAGNI）
- 禁止因为"未来可能"而创建 Factory/Strategy/Registry/SPI/Plugin
- 禁止一步到位抽象（只有一种实现时直接实现）
- 抽象后如果文件更多、调用链更长、理解成本更高，则禁止抽象
- 禁止为短期开发速度牺牲长期维护性

### 编码禁止

- 禁止吞掉异常（空 catch）
- 禁止在核心层使用技术异常类型
- 禁止魔法数字，必须提取为命名常量
- 禁止胖接口、万能接口、未来能力预留接口

---

## 项目约束

### 人员约束

<!-- 例如：单人开发、小团队开发 -->

### 技术约束

<!-- 例如：必须使用 Java、必须兼容 JDK8 -->

### 环境约束

<!-- 例如：设备仅支持 HTTP、必须离线运行 -->

---

## 测试约束

| 模块类型 | 覆盖率要求 | 重点测试 |
|----------|-----------|----------|
| 核心层 | >= 80% | 业务规则、不变量、状态流转 |
| 技术层 | >= 70% | 端口实现、错误处理、边界条件 |
| 策略层 | >= 60% | 路由逻辑、降级策略 |

测试原则：

- Arrange-Act-Assert 模式组织测试代码
- 每个测试只验证一个行为
- 测试之间互不依赖，可独立运行
- 优先测试核心层业务规则，而非实现细节
- 禁止测试私有方法，通过公共接口间接验证

状态机测试要求：

- 每个状态转换必须有独立测试用例
- 每个禁止转换必须有测试验证其被拒绝
- 修改状态机后必须全量运行状态机测试，不得只运行修改的转换

---

## 变更记录

| 日期 | 变更内容 | 变更人 | 关联变更 |
|------|----------|--------|----------|
| [初始化日期] | 初始版本 | [作者] | — |
